{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/poultry-manager run build && pnpm --filter @workspace/api-server run build:vercel",
  "outputDirectory": "artifacts/poultry-manager/dist/public",
  "framework": null,
  "functions": {
    "api/vercel.mjs": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/vercel" }
  ]
}
